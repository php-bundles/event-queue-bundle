filter:
    excluded_paths:
        - vendor/*
        - Tests/*

checks:
    php:
        code_rating: true
        duplication: true

build:
    tests:
        override:
            -
                command: phpunit --coverage-clover ./logs/clover.xml
                coverage:
                    file: logs/clover.xml
                    format: clover
